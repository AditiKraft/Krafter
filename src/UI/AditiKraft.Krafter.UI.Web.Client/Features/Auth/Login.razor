@page "/login"
@page "/Account/Login"
@using AditiKraft.Krafter.Contracts.Contracts.Auth
<RadzenRow Gap="0" Class="kr-auth-panel">

    @if (IsMobileDevice == false)
    {
        <RadzenColumn Size="12" SizeMD="4">
            <RadzenCard Class="kr-auth-intro">
                <RadzenText TextStyle="TextStyle.H3" TagName="TagName.H2" Class="rz-mb-6">Welcome!</RadzenText>
                <RadzenText TextStyle="TextStyle.H6">Join the world's largest community!</RadzenText>
                <RadzenText TextStyle="TextStyle.Body2">Let's get started with your <strong>30 days</strong> trial.</RadzenText>
            </RadzenCard>
        </RadzenColumn>
    }

    <RadzenColumn Size="12" SizeMD="8">
        <RadzenCard Class="kr-auth-form">
            <RadzenText TextAlign="TextAlign.Center" TextStyle="TextStyle.H5" TagName="TagName.H2" Class="rz-mb-6">
                Login
            </RadzenText>

            <RadzenStack JustifyContent="JustifyContent.Center" AlignItems="AlignItems.Center">
                @if (themeManager.CurrentActive == ThemeManager.ThemePreference.Light)
                {
                    <RadzenImage Path="login-with-google-dark.svg"
                                 Click="@StartGoogleLogin"
                                 class="google-login-button"
                                 Style="cursor: pointer; transition: transform 0.2s ease-in-out;">
                    </RadzenImage>
                }
                else
                {
                    <RadzenImage Path="login-with-google-light.svg"
                                 Click="@StartGoogleLogin"
                                 class="google-login-button"
                                 Style="cursor: pointer; transition: transform 0.2s ease-in-out;">
                    </RadzenImage>
                }

            </RadzenStack>

            <RadzenText TextAlign="TextAlign.Center" TextStyle="TextStyle.Body1" Class="rz-my-4">
                Or
            </RadzenText>

            <RadzenTemplateForm Data="@TokenRequestInput" Submit="@((TokenRequest __) => CreateToken(__))">
                <FluentValidationValidator Options="@(options => options.IncludeAllRuleSets())"/>
                <RadzenStack Gap="3">
                    <RadzenRow AlignItems="AlignItems.Center">
                        <RadzenColumn Size="12" SizeMD="2">
                            <RadzenLabel Text="Email" Component="Email"/>
                        </RadzenColumn>
                        <RadzenColumn Size="12" SizeMD="10">
                            <RadzenTextBox Style="width: 100%" @bind-Value="@TokenRequestInput.Email" Name="Email"/>
                        </RadzenColumn>
                        <ValidationMessage For="@(() => TokenRequestInput.Email)"/>
                    </RadzenRow>
                    <RadzenRow AlignItems="AlignItems.Center">
                        <RadzenColumn Size="12" SizeMD="2">
                            <RadzenLabel Text="Password" Component="Password"/>
                        </RadzenColumn>
                        <RadzenColumn Size="12" SizeMD="10">
                            <RadzenPassword Style="width: 100%" @bind-Value="@TokenRequestInput.Password" Name="Password"/>
                        </RadzenColumn>
                        <ValidationMessage For="@(() => TokenRequestInput.Password)"/>
                    </RadzenRow>
                    <RadzenLink Path="account/forgot-password" Text="Forgot Password?"></RadzenLink>
                </RadzenStack>
                <RadzenStack Orientation="Orientation.Horizontal" JustifyContent="JustifyContent.Right" Gap="1rem" Class="rz-mt-8 rz-mb-4">
                    <RadzenButton ButtonType="ButtonType.Submit" IsBusy="isBusy" Size="ButtonSize.Medium" Icon="login" Text="Login"/>
                </RadzenStack>
            </RadzenTemplateForm>
        </RadzenCard>
    </RadzenColumn>
</RadzenRow>
